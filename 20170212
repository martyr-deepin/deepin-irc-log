07:14 < recchan> I think Deepin-terminal needs an option to enable ligatures or to disable them (assuming gtk has something like that?). I noticed using a font with ligatures, it didn't render some characters (moving away from fonts with ligatures fixed it)
